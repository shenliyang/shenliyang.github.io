language: node_js
node_js: stable

#cache
cache:
  directories:
    - "node_modules"

# S: Build Lifecycle
install:
  - npm install

before_script:
  - npm install -g gulp 

script:
  - hexo g
  - gulp 

after_script:
  - cd ./public
  - pwd
  - echo "haha123" >> ./source/README.md
  - cat ./source/README.md
  - git init
  - git config user.name "shenliyang"
  - git config user.email ""
  - git add .
  - git commit -m "Build by Travis CI"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - dev
env:
 global:
   - GH_REF: github.com/shenliyang/shenliyang.github.io.git

